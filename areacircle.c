//user defined area of circle.
#include<stdio.h>
int main () {
    int area,a;
    printf("enter the radius:");
    scanf("%d",&a);
    area=3.14*a*a;
    printf("the area is:%d",area);
    return 0;
}